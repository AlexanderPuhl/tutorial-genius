{
  "name": "tutorial-genius",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "XDEBUG_HOST=$(hostname -I | awk '{print $1}') docker-compose up -d --build",
    "stop": "docker-compose down",
    "restart": "docker-compose restart",
    "test": "echo \"Error: no test specified\" && exit 1",
    "wp": "XDEBUG_HOST=$(hostname -I | awk '{print $1}') docker-compose run --rm wpcli wp"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
